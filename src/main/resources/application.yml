server:
  port: 8080

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
logging:
  level:
    com.kazakova.gifusdservice.feignclient.ExchangeCurrencyClient: TRACE



